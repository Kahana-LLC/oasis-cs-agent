# Local / optional full install (not used by Vercel serverless bundle).
httpx>=0.27.0
pandas>=3.0.3
python-dotenv>=1.2.2
pydantic>=2.0
# supabase optional for legacy; fetch uses httpx PostgREST (see db/client.py)
